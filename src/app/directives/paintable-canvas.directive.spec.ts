import { PaintableCanvasDirective } from './paintable-canvas.directive';

describe('PaintableCanvasDirective', () => {
  it('should create an instance', () => {
    const directive = new PaintableCanvasDirective();
    expect(directive).toBeTruthy();
  });
});
